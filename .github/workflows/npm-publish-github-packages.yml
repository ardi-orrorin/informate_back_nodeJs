on:
  push:
    branches: [ main ]

jobs:
  Run-npm-install:
    name: npm install
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: npmInstall
      run: npm install
    - name: run dev server
      run: npm run dev
        
    
