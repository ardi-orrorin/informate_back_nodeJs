on:
  push:
    branches: [ main ]

jobs:
  Run-npm-install:
    name: npm install
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: run
      run: |
        npm install
        npm run dev
        
    
